const req = document.querySelector('form');
const signup = document.querySelector('a');
const btn = document.querySelector('button');

req.addEventListener('submit', (e) => {
    e.preventDefault();
});



